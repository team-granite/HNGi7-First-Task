const record = {
  name: "Oladayo Egbeyemi",
  id: "HNG-00535",
  lang: "JavaScript",
  email: "dasther@outlook.com"
};

const resp = `Hello World, this is ${record.name} with HNGi7 ID ${record.id} using ${record.lang} for stage 2 task and ${record.email}`;

function output(){
  console.log(resp);
}
output();