//Declearation of variable
let name = `Francis Obadiah`, ID = `HNG-00072`, language = `javascript`, email = `francisobadiahajigana@gmail.com`;
//Output Definition
let output = `Hello World, this is ${name} with HNGi7 ID ${ID} using ${language} for stage 2 task and email as ${email}`;

//defining the function to
function outputScript() {
    //logging output
    console.log(output);
}

//function call
outputScript();

