var inputs = ['Akeem Agbaje', 'HNG-03992', 'Javascript', 'agbajeolawale@outlook.com'];


let full_name = inputs[0];
let ID = inputs[1];
let lang_used = inputs[2];
let email = inputs[3];

var outputs = function() {
    
    console.log("Hello World, this is " + full_name + " with HNGi7 ID " + ID + " using "  + lang_used + " for stage 2 task and " + email);
    
    
    
}

outputs();
